// 處理 PDF 文件
export async function processPDF(): Promise<string> {
    // 實作 PDF 處理邏輯（如使用 pdf-parse 或其他 PDF 解析庫）
    // 這裡返回模擬結果
    return "PDF 內容處理結果";
}

// 處理圖片文件
export async function processImage(): Promise<string> {
    // 實作圖片 OCR 處理邏輯（如使用 Tesseract.js）
    // 這裡返回模擬結果
    return "圖片 OCR 處理結果";
} 